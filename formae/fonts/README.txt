Drop the Formae webfont file(s) here.

Expected by formae/style.css:
  fonts/Formae-Variable.woff2   (variable font, weights 100–900)

If you're testing static weights instead of a variable font, add one
@font-face block per weight in formae/style.css (see the "Formae" block
near the top) instead of the single variable-font rule.
